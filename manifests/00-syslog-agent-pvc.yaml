apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: syslog-agent-log-dest
  namespace: default
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: local-path